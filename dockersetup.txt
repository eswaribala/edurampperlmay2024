sudo apt-get update

sudo apt-get install -y docker.io
#and check 
  docker -v

sudo usermod -aG docker $USER

#check container
docker container ls -a
# create mysql server
docker run --name eduramp-mysql -e MYSQL_ROOT_PASSWORD=password -e MYSQL_DATABASE=edupatientdb -e MYSQL_USER=demo_user -e MYSQL_PASSWORD=demo_pass -d mysql
#mysql client
docker exec -it eduramp-mysql mysql -u root -p


